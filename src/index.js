import React from 'react';
import ReactDOM from 'react-dom';
// import './index.css'; // Make sure this file exists or remove this line if not needed
import App from './App'; // Ensure App.js exists

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root')
);
